import React from 'react';

export function Contact() {

    const handleSubmit = () => {
        
    }

    return (
        <section>
            <h1>Contact!</h1>
            <form onSubmit={handleSubmit(e)}>



                <p>Contact!</p>
            </form>
        </section>
    );
}